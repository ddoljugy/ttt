version: 0.2
frontend:
  phases:
    preBuild:
      commands:
        - 'npm install --legacy-peer-deps'
    build:
      commands:
        - 'npm run build'
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - 'node_modules/**/*'

backend:
  phases:
    build:
      commands:
        - npm install --legacy-peer-deps
