version: 0.2

phases:
  preBuild:
    commands:
      - npm install --legacy-peer-deps
  build:
    commands:
      - npm run build
artifacts:
  base-directory: .next
  files:
    - '**/*'
cache:
  paths:
    - node_modules/**/*
